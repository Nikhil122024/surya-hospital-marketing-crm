"use client";
import { MapPin } from "lucide-react";
import type { GPSLocation } from "@/types";
export function GoogleMap({ markers = [], currentLocation }: { markers?: GPSLocation[]; currentLocation?: GPSLocation }) { return <div className="relative min-h-[280px] overflow-hidden rounded-2xl border border-slate-200 bg-[#e9f3f5]" style={{ backgroundImage: "linear-gradient(#c9e0e0 1px, transparent 1px), linear-gradient(90deg, #c9e0e0 1px, transparent 1px)", backgroundSize: "42px 42px" }}><div className="absolute inset-0 bg-[#d7e9e7]/40"/><div className="absolute left-1/2 top-1/2 flex h-12 w-12 -translate-x-1/2 -translate-y-1/2 items-center justify-center rounded-full bg-blue-600 text-white shadow-xl ring-8 ring-blue-100"><MapPin size={20}/></div>{markers.map((marker,index) => <div key={`${marker.latitude}-${marker.longitude}`} className="absolute" style={{ left: `${20 + (index * 17) % 65}%`, top: `${25 + (index * 23) % 50}%` }}><MapPin size={21} className="fill-teal-500 text-teal-700"/><span className="absolute left-6 top-0 whitespace-nowrap rounded bg-white px-2 py-1 text-[10px] font-bold text-slate-600 shadow">{marker.label}</span></div>)}<div className="absolute bottom-4 left-4 rounded-xl bg-white/90 px-3 py-2 text-xs font-bold text-slate-600 shadow">{currentLocation ? "Live location enabled" : "Demo map · API-ready"}</div></div>; }